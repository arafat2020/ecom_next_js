"use client"
import React from 'react'

function ServerHandler({ children }: { children: React.ReactNode }) {
    return (
        <>
            {children}
        </>
    )
}

export default ServerHandler